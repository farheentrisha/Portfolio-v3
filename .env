# Admin API (required for npm run server / dev:full)
ADMIN_PASSWORD=dev-admin-pass
JWT_SECRET=dev-jwt-secret-min-32-characters-long

# Optional: API port (default 3001)
PORT=3001

# Production frontend: URL of your deployed API (leave empty for local proxy)
# VITE_API_URL=https://your-api.onrender.com
